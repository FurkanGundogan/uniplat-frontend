import { makeStyles } from "@mui/styles";

const UniversityCardStyles = makeStyles((theme) => ({
  mygrid: {
    [theme.breakpoints.down("sm")]: {
      maxWidth: "100% !important",
      maxWidth: "100% !important",
    },

  },
 
}));

export default UniversityCardStyles;
